server:
  port: 8762

eureka:
  instance:
    hostname: localhost2
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost1:8761/eureka/

spring:
  application:
    name: eurka-server